# Dashboards

Dashboard examples reference only canonical `electricity_pro_*` entities.

The standard built-in-card dashboard and installation guidance are maintained
in [`examples/dashboards`](../examples/dashboards/README.md).
